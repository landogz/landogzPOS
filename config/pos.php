<?php

return [

    /*
    |--------------------------------------------------------------------------
    | POS Touchscreen Mode
    |--------------------------------------------------------------------------
    |
    | When true, the POS UI is optimized for touchscreen/kiosk use: inputs
    | are focused when modals open so the on-screen keyboard appears
    | (e.g. on tablets or touch-only devices). Set to false for desktop.
    |
    */

    'touchscreen' => env('POS_TOUCHSCREEN', false),

];
